# Database — SQLite for local dev, PostgreSQL for production
DATABASE_URL="file:./dev.db"
# DATABASE_URL="postgresql://user:password@host:5432/nurse_scheduler"

# JWT secret — minimum 64 characters, change in production
JWT_SECRET="dev-secret-change-in-prod-64chars-minimum-xxxxxxxxxxxxxxxxxxxxxxxx"

# Node environment
NODE_ENV="development"

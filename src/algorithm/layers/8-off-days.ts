import type { Grid, AlgoNurse } from "../types";

/**
 * Layer 8: Mark empty cells as OFF.
 * Any AVAILABLE cell that wasn't filled becomes an OFF day.
 */
export function layer8_offDays(
  _grid: Grid,
  _nurses: AlgoNurse[]
): void {
  // STUB — will be implemented with full logic
}
